{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Backend Service",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/backend"
        }
    ]
}